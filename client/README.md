# Shoe Store Final (Pure HTML Frontend)

## Run
Use VS Code Live Server (recommended) on the `client/` folder, or:

```bash
cd client
python -m http.server 5500
```

Open:
- http://127.0.0.1:5500/login.html

Backend must be running on http://localhost:5000
